---
title: handleCartClick()
id: handle-cart-click
---

This action takes no arguments and toggles `shouldDisplayCart` between `true` and `false`. This can be useful if you want to implement a cart with a button to toggle it open and closed.

It is possible that in the future this may be renamed or aliased to `toggleCartDisplay()`.
