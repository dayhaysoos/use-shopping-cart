---
title: 'Getting started'
---

## Installation

```bash
npm install --save stripe use-shopping-cart

or

yarn add stripe use-shopping-cart
```

The package needs to be configured with your account's secret key, which is available in the [Stripe Dashboard](https://dashboard.stripe.com/apikeys). Require it with the key's value.

At the root level of your app, wrap your root component in the `<CartProvider />`

```jsx
import { CartProvider } from 'use-shopping-cart'
import './index.css'
import App from './App'

const stripe = require('stripe')('sk_test_...')

ReactDOM.render(
  <CartProvider
    stripe={stripe}
    billingAddressCollection={false}
    successUrl={'stripe.com'}
    cancelUrl={'twitter.com/dayhaysoos'}
    currency={'USD'}
  >
    <App />
  </CartProvider>,
  document.getElementById('root')
)
```

Once you have successfully installed `stripe` and `use-shopping-cart` and wrap your root component with CartProvider, you are ready to go! 🚀
