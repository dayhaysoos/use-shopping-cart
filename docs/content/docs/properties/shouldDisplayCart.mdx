---
title: shouldDisplayCart
id: should-display-cart
---

This property is a boolean that represents whether or not the cart should be displayed or hidden. Its value can be changed via `handleCartClick()`, `handleCartHover()`, and `handleCloseCart()`.
