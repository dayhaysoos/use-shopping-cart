---
title: handleCartHover()
id: handle-cart-hover
---

This action takes no arguments and sets `shouldDisplayCart` to `true`. This can be useful if you want to create a button that only opens the cart and have a separate way to hide the cart.

It is possible that in the future this may be renamed or aliased to `openCartDisplay()`.
